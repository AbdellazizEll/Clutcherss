{% extends 'indexback.html.twig' %}

{% block body %}


    <h1>Ajouter une catégorie</h1>

    <div class="col-lg-7 order-2 order-lg-1">

        {{form_start(fadd , {'attr' :{ 'class' : 'text-start' ,  'novalidate' : 'novalidate' } } )}}
        <div class="form-group">
            {{ form_label(fadd) }}
            {{form_widget(fadd , {'attr' :{'class' : 'form-control'}})}}
            {{ form_errors(fadd) }}
            <button class="btn btn-primary">Save<img src="img/icons/double-arrow.png" alt="#"/></button>
            {{form_end(fadd)}}
        </div>

    </div>

    {{form_end(fadd)}}
{% endblock %}