{% extends 'back/indexback.html.twig' %}

{% block body %}

	<h1>modifier catégorie</h1>
	<div class="col-lg-7 order-2 order-lg-1">

		{{form_start(fadd , {'attr' :{'novalidate' : 'novalidate' , 'class' : 'form-control' }     } )}}
		{{form_widget(fadd , {'attr' :{'class' : 'form-control'}}) }}
		<button class="btn btn-primary">Save<img src="img/icons/double-arrow.png" alt="#"/></button>
		{{form_end(fadd)}}

	</div>

	{{form_end(fadd)}}
{% endblock %}
